apiVersion: v1
items:
- apiVersion: networking.k8s.io/v1
  kind: Ingress
  metadata:
    name: ingress-hosts
    namespace: default
  spec:
    rules:
    - host: star-burger.test
      http:
        paths:
        - backend:
            service:
              name: django
              port:
                number: 80
          path: /
          pathType: Prefix
kind: List